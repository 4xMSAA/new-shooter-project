local Maid = require(shared.Common.Maid)


local SpawnerSystem = {
    UpdateRate = 1/5,
    Groups = {"Spawner"}
}

function SpawnerSystem:run(dt, entities)
    for _, entity in pairs(entities) do
    end
end
