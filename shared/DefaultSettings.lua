local DefaultSettings = {
    Particles = {
        MuzzleEnabled = true,
        CasingsEnabled = true,
    },
    Camera = {
        MoveSway = 100,
        Sensitivity = 100,
    }
}

return DefaultSettings
